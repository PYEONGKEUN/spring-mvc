<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.itbuddy.spring1.mapper.BoardMapper">

<insert id="create">
INSERT INTO board
	(
	board_no, 
	title, 
	content, 
	writer
	)
	VALUES 
	(
	NULL, 
	#{title}, 
	#{content}, 
	#{writer}
	)
</insert>

<select id="list" resultType="Board">
<![CDATA[
SELECT 
	board_no, 
	title, 
	content, 
	writer, 
	reg_date
FROM 
	board
WHERE 
	board_no > 0
ORDER BY 
	board_no DESC,
	reg_date DESC
LIMIT #{pageStart}, #{sizePerPage}
]]>

</select>

<select id="read" resultType="Board">
SELECT 
	board_no, 
	title, 
	content, 
	writer, 
	reg_date
FROM 
	board
WHERE 
	board_no = #{boardNo}
</select>

<update id="update">
UPDATE
	board
SET
	title = #{title},
	content = #{content}
WHERE
	board_no = #{boardNo}
</update>

<delete id="delete">
DELETE FROM 
	board
WHERE 
	board_no = #{BoardNo}
</delete>

<select id="count" resultType="int">
 <![CDATA[
 SELECT
 	count(board_no)
 FROM
 	board
 WHERE
 	board_no > 0
 ]]>
</select>

</mapper>
